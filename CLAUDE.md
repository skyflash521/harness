# CLAUDE.md

## 前提

- ユーザーとの会話、進捗報告、最終回答は日本語で行う。これらで述べる時刻は、文脈がタイムゾーンを
  指定しているときを除き JST で書く。
- コミットして残す成果物——文書・コミットメッセージ——も日本語で書く。識別子・フラグ名・外部ツールが
  出す文言など、対象そのものが英語であるものはそのまま用いる。
- ユーザーの指示を無批判に実行しない。間違いが混入している前提で批判的に検証すること。

## リポジトリ構成

何を提供するリポジトリかと、プラグインの導入・更新・開発環境の手順は [README.md](README.md) にある。

配布物は `plugins/<プラグイン名>/` に1つずつ置く。どのプラグインを配布するかの正本は
[marketplace.json](.claude-plugin/marketplace.json)。プラグインの中身は次の役割で分かれる。

- `.claude-plugin/plugin.json` — manifest。`version` は刻印スクリプトが生成する
- `hooks/` — フック本体と、それを登録する `hooks.json`
- `skills/`・`agents/` — スキルとエージェント。個々の役割は各ファイルの frontmatter の `description`
- `docs/criteria/` — そのプラグインが課す規約・契約の正本のうち、レビューの常設観点または機械の
  検査で適用されるもの
- `docs/guidance/` — エージェントが自分で読んだ場合にだけ適用される規約。差分に現れない振る舞い
  (着手の判断、不備の終わらせ方、判定を受け取った後の動き、指示の扱い)を定めるためゲートに
  載せられないものと、破っても取り直せば済むためゲートを要しないもの
- `contract/` — 契約を機械確認するスクリプトと、その入力になる設定
- `scripts/` — 同梱の補助スクリプト。フックの誘導を受けてエージェントが起動する
- `tests/` — 自己テストが使うフィクスチャ

リポジトリ自身の側は次のとおり。

- `scripts/` — 検査・保守スクリプト。用途と呼び出し形は各スクリプト冒頭の docstring
- `docs/conventions/` — このリポジトリの規約。通す検査と合格条件は
  [検証手順](docs/conventions/verification.md)が定める
- `.githooks/` — コミット時に走る検査
- `.github/workflows/` — CI
- `.claude/settings.json` — permissions・sandbox・プラグインの有効化。フックの動作確認のための
  一時的な登録先は `.claude/settings.local.json`。**要らなくなったら直ちに消す**
- `.scratch/` — コミットしない使い捨てのスクリプト・一時ドキュメントの置き場。除外設定済みで
  追跡されない。**不要になった時点で削除する**
- リポジトリルート直下 — 各検査器の設定(ドット始まりのファイルを含む)

何があるかは `git ls-files`、それが何をするかは各ファイルの docstring・frontmatter・冒頭コメントで
分かる。

## プラグインのバージョン

`plugins/` 配下を変更したら、**ステージした後・レビューを回す前**に刻印する。

```sh
python3 scripts/stamp_plugin_version.py
```

この順序が定めるのは**レビューを実際に開始するときの並び**であって、開始の許可ではない。レビューは
ユーザーが指示したときだけ回す。**指示が出ていない間も、承認待ち等でゲートを通れない間も、その準備
としてステージも刻印もしない。**

刻印スクリプトが「刻印済みのためスキップ」と告げたのに、**自分でその刻印をした覚えが無ければ、
そのまま進めずに調べる。** 同じ作業ツリーで別のセッションが動いている兆候にあたる。
