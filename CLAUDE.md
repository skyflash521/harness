# CLAUDE.md

## 会話ルール

- ユーザーとの会話、進捗報告、最終回答は日本語で行う。
- ユーザーの指示を無批判に実行しない。間違いが混入している前提で批判的に検証すること。

## 一時ファイル

コミットしない使い捨てのスクリプトやドキュメントは `.scratch/` に置く。

## プラグインのバージョン

`plugins/` 配下を変更したら、**ステージした後・レビューを回す前**に刻印する。

```sh
python3 scripts/stamp_plugin_version.py
```

刻印はステージ差分を変えるので、レビューの後に実行するとそのレビューが現在の差分に対する
ものでなくなり、取り直しになる。

## リポジトリ概要

概要は [README.md](README.md) を参照。
