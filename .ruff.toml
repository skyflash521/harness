[lint]
# 採用する規則をここで定める。既定の規則集合は ruff のバージョンで変わり、固定しないと更新の
# たびに既存コードへ新しい規則が当たって CI が落ちる。この集合は 0.12 の既定と同じ。
# 0.12 より後に既定へ加わり 0.16 の既定に含まれる規則は全件を確認し、フックの fail-open や
# returncode を自前で見る書き方など意図した設計に当たるもので、欠陥ではなかったため採らない。
select = ["E4", "E7", "E9", "F"]
