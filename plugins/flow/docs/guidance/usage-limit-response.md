# 使用量上限への応答規約

使用量上限(利用上限への到達)で処理が失敗したときに、**次に何をしてよいかの正本**とする。

## 1. 適用範囲

**使用量上限を理由に処理が失敗した場面すべて**を対象とする。レビューの場面に限らない——自分が直接
受けた失敗、サブエージェントが返した失敗、委譲先のプロセスが返した失敗を問わない。

**対象外は、枠が尽きたのではなく使えないことを示す失敗**(モデルの利用不可・モデル未対応・権限
エラー)である。これらは実行中のスキルの定めに従う。

## 2. 枠は次の順で決める

**先に経路で分け、残りをエラー本文の名指しで分ける。** この順のとおりに判定し、他の材料を混ぜない。

1. **失敗を返した経路が Codex なら、[別プロセスの枠](#5-別プロセスの枠はそのプロセス向けの手順に従う)。** 経路で決まるので本文は見ない。
2. **それ以外は、エラー本文が Fable を名指ししているかだけで分ける。**
   - **名指しが無ければ Fable の枠ではない。** Claude 全体の枠として[§3](#3-claude-全体の枠は時刻を見ずに取り直す)のとおり扱う。
   - **名指しがあるときだけ**、Fable 専用の別枠として[§4](#4-fable-専用の別枠は繰り返さず報告して止める)のとおり扱う。

**名指しとは、尽きた枠のモデルが Fable であるとエラー本文が述べていること**(`Fable` / `fable` /
`claude-fable-…` が、上限に達した対象としてその文に現れること)をいう。**エージェント名・スキル名・
起動時に自分が付けた名前・ファイルパスの一部として現れる `fable`**(`flow:fable-reviewer`・
`fable-review-loop` など)**は名指しに数えない**——それは何を起動したかであって、どの枠が尽きたかを
述べていない。

**本文の名指し以外を判定に使わない。** 失敗した呼び出しのモデルが Fable であること・実行中のループが
Fable のループであること・エージェントの名前は、いずれも根拠にならない——**Claude 全体の枠が尽きて
いても、Fable のサブエージェントは同じ形で失敗する**。名指しの無いエラーを Fable の上限と読むのは
この取り違えであり、禁じる。

## 3. Claude 全体の枠は時刻を見ずに取り直す

**リセット時刻を読み取らない。残り時間を計算しない。出力へ書かない。時刻を根拠にユーザーへ諮らない。**
エラーや通知が時刻を示していても使わない。

**自分がそのエラーを読んで考えて書けている事実が、この枠がいま効いていない証拠である**——効いて
いれば動けない。時刻は行動を変えない情報で、調べた時点で無駄が確定する。失敗した処理はそのまま
取り直す。**取り直すのは失敗した処理であって、そこまでに積み上げた文脈ではない**——名前で再開できる
サブエージェントのように**継続できる経路なら、継続のまま同じ依頼を送り直す**。上限は処理の中身の
不備ではないので会話コンテキストを疑う理由が無く、捨てて起動し直せば指示文と前ラウンドまでの文脈の
再送でこの枠を余計に使う。**取り直しても同じ失敗が返ることだけが意味のある信号**で、そのときも時刻は要らない。
何回で諦めるかは実行中のスキルの停止規定が定め、**その規定が無い場面では、取り直して同じ上限が
返った時点で報告して止まる**。

## 4. Fable 専用の別枠は繰り返さず報告して止める

こちらが動けていても、この枠だけが尽きていることはありうるので、動けていることは証拠にならない。
一方で**取り直しても同じ上限に当たるだけなので、再試行を繰り返さない**。上限に達した旨を報告して
止める(エラーが再試行可能になる時刻を示していれば、そのまま添えてよい)。

## 5. 別プロセスの枠はそのプロセス向けの手順に従う

Codex は別プロセスなので、こちらが動いていても向こうが止まっていることがありうる。リセット時刻を
読んで待つ手順は [flow:codex-watchdog の使用量上限で待つときの手順](../../skills/codex-watchdog/SKILL.md#使用量上限で待つときの手順)が正本。**この待機の手順を
Claude 側の枠へ持ち込まない。**

## 6. 上限を理由に指定を動かさない

どの枠でも、上限は指定を動かす根拠にならない([指定は自分の判断で動かさない](user-directives.md#2-指定は自分の判断で動かさない)が正本)。
